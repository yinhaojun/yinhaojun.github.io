<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Room预填充数据库</title>
    <url>/2020/01/07/Room-prepopulate/</url>
    <content><![CDATA[<p>有时，您可能希望您的应用程序从已经加载了一组特定数据的数据库开始，这称为预填充数据库。在Room2.2.0及更高版本中，可以使用API方法在初始化时使用设备文件系统中预打包的数据库文件中的内容预填充Room数据库。</p>
<p><strong><em>注意：通过<code>Room.inMemoryDatabaseBuilder()</code>得到的数据库不支持使用<code>createFromAsset()</code>或<code>createFromFile()</code>预填充数据库。</em></strong></p>
<a id="more"></a>
<h2 id="从asset预填充"><a href="#从asset预填充" class="headerlink" title="从asset预填充"></a>从asset预填充</h2><p>要从位于应用程序<code>asset/</code>目录中任何位置的预打包数据库文件中预填充Room数据库，请在调用build()之前从RoomDatabase.Builder对象中调用<code>createFromAsset()</code>方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Room.databaseBuilder(appContext, AppDatabase.class, <span class="string">"Sample.db"</span>)</span><br><span class="line">    .createFromAsset(<span class="string">"database/myapp.db"</span>)</span><br><span class="line">    .build();</span><br></pre></td></tr></table></figure>
<p><code>createFromAsset()</code>方法接受一个字符串参数，该参数包含从assets /目录到预打包数据库文件的相对路径。</p>
<p>Note: When prepopulating from an asset, Room validates the database to ensure that its schema matches the schema of the prepackaged database. You should export your database’s schema to use as a reference as you create the prepackaged database file.</p>
<p><strong><em>注意：从<code>asset</code>预填充时，Room会验证数据库，以确保其结构与预打包数据库的结构(schema)匹配。 创建预打包的数据库文件时，应导出数据库的结构以用作参考。Schema，指的是一组DDL语句集，该语句集完整地描述了数据库的结构。</em></strong></p>
<h2 id="从文件系统中预填充"><a href="#从文件系统中预填充" class="headerlink" title="从文件系统中预填充"></a>从文件系统中预填充</h2><p>要从位于设备文件系统中除应用程序<code>asset/</code>目录之外的任何位置的预打包数据库文件中预填充Room数据库，请在调用build（）之前从RoomDatabase.Builder对象中调用<code>createFromFile()</code>方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Room.databaseBuilder(appContext, AppDatabase.class, <span class="string">"Sample.db"</span>)</span><br><span class="line">    .createFromFile(<span class="keyword">new</span> File(<span class="string">"mypath"</span>))</span><br><span class="line">    .build();</span><br></pre></td></tr></table></figure>
<p><code>createFromFile()</code>方法接受预打包数据库文件的File参数。 Room会创建指定文件的副本，而不是直接打开它，因此请确保您的应用对文件具有读取权限。</p>
<h2 id="处理包括预打包数据库的迁移"><a href="#处理包括预打包数据库的迁移" class="headerlink" title="处理包括预打包数据库的迁移"></a>处理包括预打包数据库的迁移</h2><p>预打包的数据库文件还可以更改Room数据库处理回退迁移的方式。 通常，启用破坏性迁移后，Room必须执行没有迁移路径的迁移，Room会删除数据库中的所有表，并为目标版本创建具有指定架构的空数据库。 但是，如果包括与目标版本号相同的预打包数据库文件，Room会在执行破坏性迁移后尝试使用预打包数据库文件的内容填充新创建的数据库。</p>
<p>下面的内容提供了一些如何在实践中工作的示例。</p>
<h4 id="示例：使用预打包的数据库进行回退迁移"><a href="#示例：使用预打包的数据库进行回退迁移" class="headerlink" title="示例：使用预打包的数据库进行回退迁移"></a>示例：使用预打包的数据库进行回退迁移</h4><p>假设以下内容：</p>
<ul>
<li>您在应用上定义了Room数据库，版本为3</li>
<li>​     设备上已安装的数据库实例为版本2</li>
<li>​     预打包的数据库文件的版本为3</li>
<li>​     从版本2到版本3没有实现的迁移路径</li>
<li>​     破坏性迁移(<strong>Destructive migrations</strong>)已启用。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义数据库的版本是 3.</span></span><br><span class="line"><span class="meta">@Database</span>(version = <span class="number">3</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AppDatabase</span> <span class="keyword">extends</span> <span class="title">RoomDatabase</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启用Destructive migrations并提供了预打包的数据myapp.db</span></span><br><span class="line">Room.databaseBuilder(appContext, AppDatabase.class, <span class="string">"Sample.db"</span>)</span><br><span class="line">    .createFromAsset(<span class="string">"database/myapp.db"</span>)</span><br><span class="line">    .fallbackToDestructiveMigration()</span><br><span class="line">    .build();</span><br></pre></td></tr></table></figure>
<p>在这种情况下会发生以下情况：</p>
<ul>
<li>由于您的应用程序中定义的数据库为版本3，而设备上已安装的数据库实例为版本2，因此必须进行迁移</li>
<li>由于没有从版本2到版本3的已实施迁移计划，因此迁移是回退迁移。</li>
<li>因为调用fallbackToDestructiveMigration()构建器方法，所以回退迁移是破坏性的。 Room会删除设备上已安装的数据库实例。</li>
<li>由于预打包的数据库文件的版本为3，因此Room会重新创建数据库并使用预打包的数据库文件的内容填充它。 另一方面，如果您预打包的数据库文件位于版本2上，则Room将注意该文件与目标版本不匹配，因此不会将其用作回退迁移的一部分（也就是不会将预打包数据库的内容填充到新的数据库中）。</li>
</ul>
<h4 id="示例：使用预打包的数据库实施迁移"><a href="#示例：使用预打包的数据库实施迁移" class="headerlink" title="示例：使用预打包的数据库实施迁移"></a>示例：使用预打包的数据库实施迁移</h4><p>假设您的应用实现了从版本2到版本3的迁移路径：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Database class definition declaring version 3.</span></span><br><span class="line"><span class="meta">@Database</span>(version = <span class="number">3</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AppDatabase</span> <span class="keyword">extends</span> <span class="title">RoomDatabase</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Migration path definition from version 2 to version 3.</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> Migration MIGRATION_2_3 = <span class="keyword">new</span> Migration(<span class="number">2</span>, <span class="number">3</span>) &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">migrate</span><span class="params">(SupportSQLiteDatabase database)</span> </span>&#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// A prepackaged database is provided.</span></span><br><span class="line">Room.databaseBuilder(appContext, AppDatabase.class, <span class="string">"Sample.db"</span>)</span><br><span class="line">    .createFromAsset(<span class="string">"database/myapp.db"</span>)</span><br><span class="line">    .addMigrations(MIGRATION_2_3)</span><br><span class="line">    .build();</span><br></pre></td></tr></table></figure>
<p>在这种情况下会发生以下情况：</p>
<ul>
<li>由于您的应用程序中定义的数据库为版本3，而设备上已安装的数据库为版本2，因此必须进行迁移</li>
<li>由于存在从版本2到版本3的已实现迁移路径，因此Room运行定义的migration（）方法将设备上的数据库实例更新到版本3，从而保留数据库中已经存在的数据。 Room不使用预打包的数据库文件，因为Room仅在回退迁移的情况下才使用预打包的数据库文件</li>
</ul>
<h4 id="示例：使用预打包的数据库进行多步迁移"><a href="#示例：使用预打包的数据库进行多步迁移" class="headerlink" title="示例：使用预打包的数据库进行多步迁移"></a>示例：使用预打包的数据库进行多步迁移</h4><p>预打包数据库文件还可能影响包含多个步骤的迁移。 考虑以下情况：</p>
<ul>
<li>您在应用上定义了Room数据库版本为4</li>
<li>设备上已安装的数据库实例为版本2</li>
<li>预打包数据库文件的版本为3</li>
<li>从版本3到版本4有一个已实现的迁移路径，但没有从版本2到版本3的迁移路径</li>
<li>破坏性迁移已启用</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Database class definition declaring version 4.</span></span><br><span class="line"><span class="meta">@Database</span>(version = <span class="number">4</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AppDatabase</span> <span class="keyword">extends</span> <span class="title">RoomDatabase</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Migration path definition from version 3 to version 4.</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> Migration MIGRATION_3_4 = <span class="keyword">new</span> Migration(<span class="number">3</span>, <span class="number">4</span>) &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">migrate</span><span class="params">(SupportSQLiteDatabase database)</span> </span>&#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Destructive migrations are enabled and a prepackaged database is</span></span><br><span class="line"><span class="comment">// provided.</span></span><br><span class="line">Room.databaseBuilder(appContext, AppDatabase.class, <span class="string">"Sample.db"</span>)</span><br><span class="line">    .createFromAsset(<span class="string">"database/myapp.db"</span>)</span><br><span class="line">    .addMigrations(MIGRATION_3_4)</span><br><span class="line">    .fallbackToDestructiveMigration()</span><br><span class="line">    .build();</span><br></pre></td></tr></table></figure>
<p>在这种情况下会发生以下情况：</p>
<ul>
<li>由于您的应用程序中定义的数据库为版本4，而设备上已安装的数据库实例为版本2，因此必须进行迁移</li>
<li>由于没有从版本2到版本3的已实现迁移路径，因此迁移是回退迁移</li>
<li>因为调用fallbackToDestructiveMigration（）构建器方法，所以回退迁移是破坏性的。 Room将设备上的数据库实例删除</li>
<li>由于预打包数据库文件的版本为3，因此Room会重新创建数据库并使用预打包的数据库文件的内容填充它</li>
<li>设备上安装的数据库现在的版本为3。由于它仍低于应用程序中定义的版本，因此必须进行另一次迁移</li>
<li>由于存在从版本3到版本4的已实现迁移路径，因此Room运行定义的<code>migration()</code>方法将设备上的数据库实例更新到版本4，从而保留从版本3预打包的数据库文件复制过来的数据。</li>
</ul>
]]></content>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack</tag>
        <tag>Room</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 Room DAO 访问数据</title>
    <url>/2020/01/07/Room-accessing-data/</url>
    <content><![CDATA[<p>要使用 Room 访问应用的数据，您需要使用数据访问对象 (DAO)。这些Dao对象构成了Room的主要组件，因为每个DAO都包含一些方法，这些方法提供对应用数据库的抽象访问权限。</p>
<p>通过使用DAO类（而不是查询构建器或直接查询）访问数据库，您可以拆分数据库架构的不同组件。</p>
<p>DAO既可以是接口，也可以是抽象类。如果是抽象类，则该DAO可以选择有一个以<code>RoomDatabase</code>为唯一参数的构造函数，Room会在编译时创建每个DAO实现。</p>
<p><strong><em>注意：除非您对构建器调用 allowMainThreadQueries()，否则 Room 不支持在主线程上访问数据库，因为它可能会长时间锁定界面。异步查询（返回 LiveData 或 Flowable 实例的查询）无需遵守此规则，因为此类查询会根据需要在后台线程上异步运行查询。</em></strong></p>
<a id="more"></a>
<h2 id="定义方法以方便使用"><a href="#定义方法以方便使用" class="headerlink" title="定义方法以方便使用"></a>定义方法以方便使用</h2><p>您可以使用 DAO 类表示多个便捷查询。本文档提供了几个常见示例。</p>
<h3 id="Insert"><a href="#Insert" class="headerlink" title="Insert"></a>Insert</h3><p>当您创建 DAO 方法并使用 <code>@Insert</code>对其进行注释时，Room 会生成一个实现，该实现在单个事务中将所有参数插入到数据库中。</p>
<p>以下代码段展示了几个示例查询：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Insert</span>(onConflict = OnConflictStrategy.REPLACE)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insertUsers</span><span class="params">(User... users)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Insert</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insertBothUsers</span><span class="params">(User user1, User user2)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Insert</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insertUsersAndFriends</span><span class="params">(User user, List&lt;User&gt; friends)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果 <code>@Insert</code>方法只接收 1 个参数，则可返回 <code>long</code>，这是插入项的新 <code>rowId</code>。如果参数是数组或集合，则应返回 <code>long[]</code> 或 <code>List&lt;Long&gt;</code>。</p>
<h3 id="Update"><a href="#Update" class="headerlink" title="Update"></a>Update</h3><p><code>Update</code>便捷方法会修改数据库中以参数形式给出的一组实体。它使用与每个实体的主键匹配的查询。</p>
<p>以下代码段演示了如何定义此方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Update</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">updateUsers</span><span class="params">(User... users)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虽然通常没有必要，但您可以让此方法返回一个 <code>int</code> 值，表示数据库中更新的行数。</p>
<h3 id="Delete"><a href="#Delete" class="headerlink" title="Delete"></a>Delete</h3><p><code>Delete</code>便捷方法会从数据库中删除一组以参数形式给出的实体。它使用主键查找要删除的实体。</p>
<p>以下代码段演示了如何定义此方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Delete</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">deleteUsers</span><span class="params">(User... users)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虽然通常没有必要，但您可以让此方法返回一个 <code>int</code> 值，表示从数据库中删除的行数。</p>
<h2 id="Query"><a href="#Query" class="headerlink" title="Query"></a>Query</h2><p><code>@Query</code>是 DAO 类中使用的主要注释。它允许您对数据库执行读/写操作。每个 <code>@Query</code>方法都会在编译时进行验证，因此如果查询出现问题，则会发生编译错误，而不是运行时失败。</p>
<p>Room 还会验证查询的返回值，这样的话，当返回的对象中的字段名称与查询响应中的对应列名称不匹配时，Room 会通过以下两种方式之一提醒您：</p>
<ul>
<li>如果只有部分字段名称匹配，则会发出警告</li>
<li>如果没有任何字段名称匹配，则会发出错误</li>
</ul>
<h3 id="简单查询"><a href="#简单查询" class="headerlink" title="简单查询"></a>简单查询</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT * FROM user"</span>)</span><br><span class="line">    <span class="keyword">public</span> User[] loadAllUsers();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这是一个极其简单的查询，可加载所有用户。在编译时，Room 知道它在查询用户表中的所有列。如果查询包含语法错误，或者数据库中没有用户表格，则 Room 会在您的应用编译时显示包含相应消息的错误。</p>
<h3 id="将参数传递给查询"><a href="#将参数传递给查询" class="headerlink" title="将参数传递给查询"></a>将参数传递给查询</h3><p>在大多数情况下，您需要将参数传递给查询以执行过滤操作，例如仅显示某个年龄以上的用户。要完成此任务，请在 Room 注释中使用方法参数，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT * FROM user WHERE age &gt; :minAge"</span>)</span><br><span class="line">    <span class="keyword">public</span> User[] loadAllUsersOlderThan(<span class="keyword">int</span> minAge);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在编译时处理此查询时，Room 会将 <code>:minAge</code> 绑定参数与 <code>minAge</code> 方法参数相匹配。Room 通过参数名称进行匹配。如果有不匹配的情况，则应用编译时会出现错误。</p>
<p>您还可以在查询中传递多个参数或多次引用这些参数，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT * FROM user WHERE age BETWEEN :minAge AND :maxAge"</span>)</span><br><span class="line">    <span class="keyword">public</span> User[] loadAllUsersBetweenAges(<span class="keyword">int</span> minAge, <span class="keyword">int</span> maxAge);</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT * FROM user WHERE first_name LIKE :search "</span> +</span><br><span class="line">           <span class="string">"OR last_name LIKE :search"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;User&gt; <span class="title">findUserWithName</span><span class="params">(String search)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="返回列的子集"><a href="#返回列的子集" class="headerlink" title="返回列的子集"></a>返回列的子集</h3><p>大多数情况下，您只需获取实体的几个字段。例如，您的界面可能仅显示用户的名字和姓氏，而不是用户的每一条详细信息。通过仅提取应用界面中显示的列，您可以节省宝贵的资源，并且您的查询也能更快完成。</p>
<p>借助 Room，您可以从查询中返回任何基于 Java 的对象，前提是结果列集合会映射到返回的对象。例如，您可以创建以下基于 Java 的普通对象 (POJO) 来获取用户的名字和姓氏：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">NameTuple</span> </span>&#123;</span><br><span class="line">    <span class="meta">@ColumnInfo</span>(name = <span class="string">"first_name"</span>)</span><br><span class="line">    <span class="keyword">public</span> String firstName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ColumnInfo</span>(name = <span class="string">"last_name"</span>)</span><br><span class="line">    <span class="meta">@NonNull</span></span><br><span class="line">    <span class="keyword">public</span> String lastName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>现在，您可以在查询方法中使用此 POJO：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT first_name, last_name FROM user"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;NameTuple&gt; <span class="title">loadFullName</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Room 知道该查询会返回 <code>first_name</code> 和 <code>last_name</code> 列的值，并且这些值会映射到 <code>NameTuple</code> 类的字段。因此，Room 可以生成正确的代码。如果查询返回太多的列，或者返回 <code>NameTuple</code> 类中不存在的列，则 Room 会显示一条警告。</p>
<p><strong><em>注意：这些 POJO 也可以使用 @Embedded 注释。</em></strong></p>
<h3 id="传递参数的集合"><a href="#传递参数的集合" class="headerlink" title="传递参数的集合"></a>传递参数的集合</h3><p>您的部分查询可能要求您传入数量不定的参数，参数的确切数量要到运行时才知道。例如，您可能希望从部分区域中检索所有用户的相关信息。Room 知道参数何时表示集合，并根据提供的参数数量在运行时自动将其展开。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT first_name, last_name FROM user WHERE region IN (:regions)"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;NameTuple&gt; <span class="title">loadUsersFromRegions</span><span class="params">(List&lt;String&gt; regions)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="可观察查询"><a href="#可观察查询" class="headerlink" title="可观察查询"></a>可观察查询</h3><p>执行查询时，您通常会希望应用的界面在数据发生变化时自动更新。为此，请在查询方法说明中使用 <code>LiveData</code>类型的返回值。当数据库更新时，Room 会生成更新<code>LiveData</code>所必需的所有代码。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT first_name, last_name FROM user WHERE region IN (:regions)"</span>)</span><br><span class="line">    <span class="keyword">public</span> LiveData&lt;List&lt;User&gt;&gt; loadUsersFromRegionsSync(List&lt;String&gt; regions);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong><em>注意：自版本 1.0 起，Room 会根据在查询中访问的表格列表决定是否更新 LiveData 实例。</em></strong></p>
<h3 id="使用-RxJava-进行响应式查询"><a href="#使用-RxJava-进行响应式查询" class="headerlink" title="使用 RxJava 进行响应式查询"></a>使用 RxJava 进行响应式查询</h3><p>Room 为 RxJava2 类型的返回值提供了以下支持：</p>
<ul>
<li><code>@Query</code> 方法：Room 支持 <code>Publisher</code>、<code>Flowable</code>和<code>Observable</code>类型的返回值。</li>
<li><code>@Insert</code>、<code>@Update</code>和 <code>@Delete</code> 方法：Room 2.1.0 及更高版本支持 <code>Completable</code>、<code>Single</code> 和 <code>Maybe</code> 类型的返回值。</li>
</ul>
<p>要使用此功能，请在应用的 <code>build.gradle</code> 文件中添加最新版本的 <strong>rxjava2</strong> 工件：</p>
<p>app/build.gradle</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">    def room_version = <span class="string">"2.1.0"</span></span><br><span class="line">    implementation <span class="string">'androidx.room:room-rxjava2:$room_version'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以下代码段展示了几个如何使用这些返回类型的示例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT * from user where id = :id LIMIT 1"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Flowable&lt;User&gt; <span class="title">loadUserById</span><span class="params">(<span class="keyword">int</span> id)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Emits the number of users added to the database.</span></span><br><span class="line">    <span class="meta">@Insert</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Maybe&lt;Integer&gt; <span class="title">insertLargeNumberOfUsers</span><span class="params">(List&lt;User&gt; users)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Makes sure that the operation finishes successfully.</span></span><br><span class="line">    <span class="meta">@Insert</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Completable <span class="title">insertLargeNumberOfUsers</span><span class="params">(User... users)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Emits the number of users removed from the database. Always emits at</span></span><br><span class="line"><span class="comment">       least one user. */</span></span><br><span class="line">    <span class="meta">@Delete</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Single&lt;Integer&gt; <span class="title">deleteUsers</span><span class="params">(List&lt;User&gt; users)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="直接光标访问"><a href="#直接光标访问" class="headerlink" title="直接光标访问"></a>直接光标访问</h3><p>如果应用的逻辑需要直接访问返回行，您可以从查询返回 <code>Cursor</code> 对象，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT * FROM user WHERE age &gt; :minAge LIMIT 5"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Cursor <span class="title">loadRawUsersOlderThan</span><span class="params">(<span class="keyword">int</span> minAge)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong><em>注意：强烈建议您不要使用 Cursor API，因为它无法保证行是否存在或者行包含哪些值。只有当您已具有需要光标且无法轻松重构的代码时，才使用此功能。</em></strong></p>
<h3 id="查询多个表格"><a href="#查询多个表格" class="headerlink" title="查询多个表格"></a>查询多个表格</h3><p>您的部分查询可能需要访问多个表格才能计算出结果。借助 Room，您可以编写任何查询，因此您也可以联接表格。此外，如果响应是可观察数据类型（如 <code>Flowable</code> 或 <code>LiveData</code>），Room 会观察查询中引用的所有表格，以确定是否存在无效表格。</p>
<p>以下代码段展示了如何执行表格联接来整合两个表格的信息：一个表格包含当前借阅图书的用户，另一个表格包含当前处于已被借阅状态的图书的数据。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT * FROM book "</span> +</span><br><span class="line">           <span class="string">"INNER JOIN loan ON loan.book_id = book.id "</span> +</span><br><span class="line">           <span class="string">"INNER JOIN user ON user.id = loan.user_id "</span> +</span><br><span class="line">           <span class="string">"WHERE user.name LIKE :userName"</span>)</span><br><span class="line">   <span class="function"><span class="keyword">public</span> List&lt;Book&gt; <span class="title">findBooksBorrowedByNameSync</span><span class="params">(String userName)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>您还可以从这些查询中返回 POJO。例如，您可以编写一条加载某位用户及其宠物名字的查询，如下所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyDao</span> </span>&#123;</span><br><span class="line">   <span class="meta">@Query</span>(<span class="string">"SELECT user.name AS userName, pet.name AS petName "</span> +</span><br><span class="line">          <span class="string">"FROM user, pet "</span> +</span><br><span class="line">          <span class="string">"WHERE user.id = pet.user_id"</span>)</span><br><span class="line">   <span class="keyword">public</span> LiveData&lt;List&lt;UserPet&gt;&gt; loadUserAndPetNames();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// You can also define this class in a separate file, as long as you add the</span></span><br><span class="line">   <span class="comment">// "public" access modifier.</span></span><br><span class="line">   <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">UserPet</span> </span>&#123;</span><br><span class="line">       <span class="keyword">public</span> String userName;</span><br><span class="line">       <span class="keyword">public</span> String petName;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p> 本指南也适用于带有 <code>@Transaction</code> 注释的 DAO 方法。您可以使用此功能通过其他 DAO 方法构建暂停数据库方法。然后，这些方法会在单个数据库事务中运行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line">   <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">UsersDao</span> </span>&#123;</span><br><span class="line">       <span class="meta">@Transaction</span></span><br><span class="line">       <span class="function">open suspend fun <span class="title">setLoggedInUser</span><span class="params">(loggedInUser: User)</span> </span>&#123;</span><br><span class="line">           deleteUser(loggedInUser)</span><br><span class="line">           insertUser(loggedInUser)</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="meta">@Query</span>(<span class="string">"DELETE FROM users"</span>)</span><br><span class="line">       <span class="function"><span class="keyword">abstract</span> fun <span class="title">deleteUser</span><span class="params">(user: User)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">       @Insert</span></span><br><span class="line"><span class="function">       <span class="keyword">abstract</span> suspend fun <span class="title">insertUser</span><span class="params">(user: User)</span></span></span><br><span class="line"><span class="function">   &#125;</span></span><br></pre></td></tr></table></figure>
<p><strong><em>注意：应避免在单个数据库事务中执行额外的应用端工作，因为 Room 会将此类事务视为独占事务，并且按顺序每次仅执行一个事务。也就是说，包含不必要操作的事务很容易锁定您的数据库并影响性能。</em></strong></p>
]]></content>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack</tag>
        <tag>Room</tag>
      </tags>
  </entry>
  <entry>
    <title>Room定义对象之间的关系</title>
    <url>/2020/01/07/Room-relationships/</url>
    <content><![CDATA[<p>由于 SQLite 是关系型数据库，因此您可以指定各个对象之间的关系。尽管大多数对象关系映射库都允许实体对象互相引用，但 Room 明确禁止这样做。</p>
<h2 id="定义一对多关系"><a href="#定义一对多关系" class="headerlink" title="定义一对多关系"></a>定义一对多关系</h2><p>即使您不能使用直接关系，Room 仍允许您定义实体之间的外键约束。</p>
<p>例如，如果存在另一个名为 <code>Book</code> 的实体，您可以使用 <code>@ForeignKey</code>注释定义该实体与 <code>User</code> 实体的关系，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span>(foreignKeys = <span class="meta">@ForeignKey</span>(entity = User.class,</span><br><span class="line">                                      parentColumns = <span class="string">"id"</span>,</span><br><span class="line">                                      childColumns = <span class="string">"user_id"</span>))</span><br><span class="line">    <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Book</span> </span>&#123;</span><br><span class="line">        <span class="meta">@PrimaryKey</span> <span class="keyword">public</span> <span class="keyword">int</span> bookId;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> String title;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@ColumnInfo</span>(name = <span class="string">"user_id"</span>) <span class="keyword">public</span> <span class="keyword">int</span> userId;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>由于零个或更多个 <code>Book</code> 实例可以通过 <code>user_id</code> 外键关联到一个 <code>User</code> 实例，因此这会在 <code>User</code> 和 <code>Book</code> 之间构建一对多关系模型。</p>
<p>外键非常强大，可让您指定引用的实体更新后会发生什么。例如，您可以通过在<code>@ForeignKey</code>注释中添加<code>onDelete = CASCADE</code>，在 <code>User</code> 的对应实例删除后告知 SQLite 删除该用户的所有图书。</p>
<p><strong><em>注意：SQLite 将 <code>@Insert(onConflict = REPLACE)</code>作为一组 <code>REMOVE</code> 和 <code>REPLACE</code> 操作（而不是单个 <code>UPDATE</code> 操作）处理。这种替换冲突值的方法可能会影响您的外键约束。</em></strong></p>
<a id="more"></a>
<h2 id="创建嵌套对象"><a href="#创建嵌套对象" class="headerlink" title="创建嵌套对象"></a>创建嵌套对象</h2><p>有时，您可能希望在数据库逻辑中将某个实体或数据对象表示为一个紧密的整体，即使该对象包含多个字段也是如此。在这些情况下，您可以使用 <code>@Embedded</code>注释表示要解构到表中其子字段的对象。然后，您可以像查询其他各个列一样查询嵌套字段。</p>
<p>例如，您的 <code>User</code> 类可以包含类型 <code>Address</code> 的字段，该类型表示一组分别名为 <code>street</code>、<code>city</code>、<code>state</code> 和 <code>postCode</code> 的字段。要在表中单独存储组成的列，请在 <code>User</code> 类（使用<code>@Embedded</code>注释）中添加 <code>Address</code> 字段，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Address</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String street;</span><br><span class="line">    <span class="keyword">public</span> String state;</span><br><span class="line">    <span class="keyword">public</span> String city;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ColumnInfo</span>(name = <span class="string">"post_code"</span>) <span class="keyword">public</span> <span class="keyword">int</span> postCode;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="meta">@PrimaryKey</span> <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String firstName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Embedded</span> <span class="keyword">public</span> Address address;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后，表示 <code>User</code> 对象的表会包含具有以下名称的列：<code>id</code>、<code>firstName</code>、<code>street</code>、<code>state</code>、<code>city</code> 和 <code>post_code</code>。</p>
<p><strong>注意：嵌套字段还可以包含其他嵌套字段。</strong></p>
<p>如果某个实体具有同一类型的多个嵌套字段，您可以通过设置<code>prefix</code>属性确保每个列都独一无二。然后，Room 会将提供的值添加到嵌套对象中每个列名称的开头。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">For the example above, <span class="keyword">if</span> we<span class="string">'ve written:</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">   @Embedded(prefix = "loc_")</span></span><br><span class="line"><span class="string">   Coordinates coordinates;</span></span><br><span class="line"><span class="string"> </span></span><br><span class="line"><span class="string">The column names for latitude and longitude will be loc_latitude and loc_longitude respectively.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">By default, prefix is the empty string.</span></span><br></pre></td></tr></table></figure>
<h2 id="定义多对多关系"><a href="#定义多对多关系" class="headerlink" title="定义多对多关系"></a>定义多对多关系</h2><p>您通常希望在关系型数据库中构建的另一种关系模型是两个实体之间的多对多关系，其中每个实体都可以关联到另一个实体的零个或更多个实例。例如，假设有一个音乐在线播放应用，用户可以在该应用中将自己喜爱的歌曲整理到播放列表中。每个播放列表都可以包含任意数量的歌曲，每首歌曲都可以包含在任意数量的播放列表中。</p>
<p>要构建这种关系的模型，您需要创建下面三个对象：</p>
<ul>
<li>播放列表的实体类</li>
<li>歌曲的实体类</li>
<li>用于保存每个播放列表中的歌曲相关信息的中间类</li>
</ul>
<p>您可以将实体类定义为独立单元：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Playlist</span> </span>&#123;</span><br><span class="line">    <span class="meta">@PrimaryKey</span> <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> String description;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Song</span> </span>&#123;</span><br><span class="line">    <span class="meta">@PrimaryKey</span> <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String songName;</span><br><span class="line">    <span class="keyword">public</span> String artistName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后，将中间类定义为包含对 <code>Song</code> 和 <code>Playlist</code> 的外键引用的实体：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span>(tableName = <span class="string">"playlist_song_join"</span>,</span><br><span class="line">        primaryKeys = &#123; <span class="string">"playlistId"</span>, <span class="string">"songId"</span> &#125;,</span><br><span class="line">        foreignKeys = &#123;</span><br><span class="line">                <span class="meta">@ForeignKey</span>(entity = Playlist.class,</span><br><span class="line">                            parentColumns = <span class="string">"id"</span>,</span><br><span class="line">                            childColumns = <span class="string">"playlistId"</span>),</span><br><span class="line">                <span class="meta">@ForeignKey</span>(entity = Song.class,</span><br><span class="line">                            parentColumns = <span class="string">"id"</span>,</span><br><span class="line">                            childColumns = <span class="string">"songId"</span>)</span><br><span class="line">                &#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PlaylistSongJoin</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> playlistId;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> songId;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这会生成一个多对多关系模型。借助该模型，您可以使用 DAO 按歌曲查询播放列表和按播放列表查询歌曲：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">PlaylistSongJoinDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Insert</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(PlaylistSongJoin playlistSongJoin)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT * FROM playlist "</span> +</span><br><span class="line">           <span class="string">"INNER JOIN playlist_song_join "</span> +</span><br><span class="line">           <span class="string">"ON playlist.id=playlist_song_join.playlistId "</span> +</span><br><span class="line">           <span class="string">"WHERE playlist_song_join.songId=:songId"</span>)</span><br><span class="line">    <span class="function">List&lt;Playlist&gt; <span class="title">getPlaylistsForSong</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> songId)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Query</span>(<span class="string">"SELECT * FROM song "</span> +</span><br><span class="line">           <span class="string">"INNER JOIN playlist_song_join "</span> +</span><br><span class="line">           <span class="string">"ON song.id=playlist_song_join.songId "</span> +</span><br><span class="line">           <span class="string">"WHERE playlist_song_join.playlistId=:playlistId"</span>)</span><br><span class="line">    <span class="function">List&lt;Song&gt; <span class="title">getSongsForPlaylist</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> playlistId)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack</tag>
        <tag>Room</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 Room 实体定义数据</title>
    <url>/2020/01/07/Room-defining-data/</url>
    <content><![CDATA[<p>使用 <strong>Room</strong>时，您可以将相关字段集定义为实体。对于每个实体，系统会在关联的 <code>Database</code>对象中创建一个表来存储这些项。您必须通过 <code>Database</code>类中的 <code>entities</code>数组引用实体类。</p>
<p>以下代码段展示了如何定义实体：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line">   <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">       <span class="meta">@PrimaryKey</span></span><br><span class="line">       <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">public</span> String firstName;</span><br><span class="line">       <span class="keyword">public</span> String lastName;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>要保留某个字段，Room 必须拥有该字段的访问权限。您可以将某个字段设为<em>public</em>，也可以为其提供 <em>getter</em> 和 <em>setter</em>。</p>
<a id="more"></a>
<h2 id="使用主键"><a href="#使用主键" class="headerlink" title="使用主键"></a>使用主键</h2><p>每个实体必须将至少 1 个字段定义为主键。即使只有 1 个字段，您仍然需要为该字段添加 <strong>@PrimaryKey</strong>注释。此外，如果您想让 Room 为实体分配自动 ID，则可以设置 <strong>@PrimaryKey</strong> 的 <em>autoGenerate</em>属性。如果实体具有复合主键，您可以使用 <strong>@Entity</strong> 注释的 <em>primaryKeys</em> 属性，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span>(primaryKeys = &#123;<span class="string">"firstName"</span>, <span class="string">"lastName"</span>&#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String firstName;</span><br><span class="line">    <span class="keyword">public</span> String lastName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>默认情况下，Room 将类名称用作数据库表名称。如果您希望表具有不同的名称，请设置 <strong>@Entity</strong> 注释的 <em>tableName</em> 属性，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span>(tableName = <span class="string">"users"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong><em>注意：SQLite 中的表名称不区分大小写。</em></strong></p>
<p>与 <strong>tableName</strong> 属性类似，Room 将字段名称用作数据库中的列名称。如果您希望列具有不同的名称，请将 <strong>@ColumnInfo</strong> 注释添加到字段，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span>(tableName = <span class="string">"users"</span>)</span><br><span class="line">   <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">       <span class="meta">@PrimaryKey</span></span><br><span class="line">       <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">       <span class="meta">@ColumnInfo</span>(name = <span class="string">"first_name"</span>)</span><br><span class="line">       <span class="keyword">public</span> String firstName;</span><br><span class="line"></span><br><span class="line">       <span class="meta">@ColumnInfo</span>(name = <span class="string">"last_name"</span>)</span><br><span class="line">       <span class="keyword">public</span> String lastName;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<h2 id="忽略字段"><a href="#忽略字段" class="headerlink" title="忽略字段"></a>忽略字段</h2><p>默认情况下，Room 会为在实体中定义的每个字段创建一个列。如果某个实体中有您不想保留的字段，则可以使用 <strong>@Ignore</strong> 为这些字段注释，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line">   <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">       <span class="meta">@PrimaryKey</span></span><br><span class="line">       <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">public</span> String firstName;</span><br><span class="line">       <span class="keyword">public</span> String lastName;</span><br><span class="line"></span><br><span class="line">       <span class="meta">@Ignore</span></span><br><span class="line">       Bitmap picture;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>如果实体继承了父实体的字段，则使用 @Entity 属性的 <em>ignoredColumns</em> 属性通常会更容易：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span>(ignoredColumns = <span class="string">"picture"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RemoteUser</span> <span class="keyword">extends</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="meta">@PrimaryKey</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">boolean</span> hasVpn;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="视图"><a href="#视图" class="headerlink" title="视图"></a>视图</h2><p>2.1.0 及更高版本的 Room 为 SQLite 数据库视图提供了支持，从而允许您将查询封装到类中。Room 将这些查询支持的类称为视图，在 DAO 中使用时，它们的行为与简单数据对象的行为相同。</p>
<p><strong><em>注意：与实体类似，您可以针对视图运行 SELECT 语句。不过，您无法针对视图运行 INSERT、UPDATE 或 DELETE 语句。</em></strong></p>
<h3 id="创建视图"><a href="#创建视图" class="headerlink" title="创建视图"></a>创建视图</h3><p>要创建视图，请将 <code>@DatabaseView</code> 注释添加到类中。将注释的值设为类应该表示的查询。</p>
<p>以下代码段提供了一个视图示例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@DatabaseView(&quot;SELECT user.id, user.name, user.departmentId,&quot; +</span><br><span class="line">              &quot;department.name AS departmentName FROM user &quot; +</span><br><span class="line">              &quot;INNER JOIN department ON user.departmentId = department.id&quot;)</span><br><span class="line">public class UserDetail &#123;</span><br><span class="line">    public long id;</span><br><span class="line">    public String name;</span><br><span class="line">    public long departmentId;</span><br><span class="line">    public String departmentName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="将视图与数据库相关联"><a href="#将视图与数据库相关联" class="headerlink" title="将视图与数据库相关联"></a>将视图与数据库相关联</h3><p>要将此视图添加为应用数据库的一部分，请在应用的 <code>@Database</code> 注释中添加 <code>views</code> 属性：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Database(entities = &#123;User.class&#125;, views = &#123;UserDetail.class&#125;,version = 1)</span><br><span class="line">public abstract class AppDatabase extends RoomDatabase &#123;</span><br><span class="line">    public abstract UserDao userDao();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="提供表搜索支持"><a href="#提供表搜索支持" class="headerlink" title="提供表搜索支持"></a>提供表搜索支持</h2><h3 id="支持全文搜索"><a href="#支持全文搜索" class="headerlink" title="支持全文搜索"></a>支持全文搜索</h3><p>如果您的应用需要通过全文搜索 (FTS) 快速访问数据库信息，请使用虚拟表（使用 FTS3 或 FTS4 SQLite <a href="https://www.sqlite.org/fts3.html" target="_blank" rel="noopener">扩展模块</a>）为您的实体提供支持。要使用 Room 2.1.0 及更高版本中提供的这项功能，请将 <strong>@Fts3</strong> 或 <strong>@Fts4</strong> 注释添加到给定实体，如以下代码段所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Use `@Fts3` only if your app has strict disk space requirements or if you</span></span><br><span class="line">   <span class="comment">// require compatibility with an older SQLite version.</span></span><br><span class="line">   <span class="meta">@Fts</span>4</span><br><span class="line">   <span class="meta">@Entity</span>(tableName = <span class="string">"users"</span>)</span><br><span class="line">   <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">       <span class="comment">// Specifying a primary key for an FTS-table-backed entity is optional, but</span></span><br><span class="line">       <span class="comment">// if you include one, it must use this type and column name.</span></span><br><span class="line">       <span class="meta">@PrimaryKey</span></span><br><span class="line">       <span class="meta">@ColumnInfo</span>(name = <span class="string">"rowid"</span>)</span><br><span class="line">       <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">       <span class="meta">@ColumnInfo</span>(name = <span class="string">"first_name"</span>)</span><br><span class="line">       <span class="keyword">public</span> String firstName;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p><strong><em>注意：启用 FTS 的表始终使用 INTEGER 类型的主键且列名称为“rowid”。如果是由 FTS 表支持的实体定义主键，则必须使用相应的类型和列名称。</em></strong></p>
<p>如果表支持以多种语言显示的内容，请使用 <code>languageId</code> 选项指定用于存储每一行语言信息的列：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Fts</span>4(languageId = <span class="string">"lid"</span>)</span><br><span class="line">   <span class="meta">@Entity</span>(tableName = <span class="string">"users"</span>)</span><br><span class="line">   <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">       <span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">       <span class="meta">@ColumnInfo</span>(name = <span class="string">"lid"</span>)</span><br><span class="line">       <span class="keyword">int</span> languageId;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>Room 提供了其他几个选项来定义由 FTS 支持的实体，包括结果排序、令牌生成器类型以及作为外部内容管理的表。</p>
<h3 id="将特定列编入索引"><a href="#将特定列编入索引" class="headerlink" title="将特定列编入索引"></a>将特定列编入索引</h3><p>如果您的应用必须支持不允许使用由 FTS3 或 FTS4 表支持的实体的 SDK 版本，您仍可以将数据库中的某些列编入索引，以加快查询速度。要为实体添加索引，请在 <code>@Entity</code>注释中添加<code>indices</code> 属性，以列出要在索引或复合索引中包含的列的名称。以下代码段演示了此注释过程：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span>(indices = &#123;<span class="meta">@Index</span>(<span class="string">"name"</span>),</span><br><span class="line">        <span class="meta">@Index</span>(value = &#123;<span class="string">"last_name"</span>, <span class="string">"address"</span>&#125;)&#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="meta">@PrimaryKey</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String firstName;</span><br><span class="line">    <span class="keyword">public</span> String address;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ColumnInfo</span>(name = <span class="string">"last_name"</span>)</span><br><span class="line">    <span class="keyword">public</span> String lastName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Ignore</span></span><br><span class="line">    Bitmap picture;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>有时，数据库中的某些字段或字段组必须是唯一的。您可以通过将<code>@Index</code> 注释的 <code>unique</code> 属性设为 true 来强制实施此唯一性属性。以下代码示例可防止表格具有包含 firstName 和 lastName 列的同一组值的两行：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span>(indices = &#123;<span class="meta">@Index</span>(value = &#123;<span class="string">"first_name"</span>, <span class="string">"last_name"</span>&#125;,</span><br><span class="line">        unique = <span class="keyword">true</span>)&#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="meta">@PrimaryKey</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ColumnInfo</span>(name = <span class="string">"first_name"</span>)</span><br><span class="line">    <span class="keyword">public</span> String firstName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ColumnInfo</span>(name = <span class="string">"last_name"</span>)</span><br><span class="line">    <span class="keyword">public</span> String lastName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Ignore</span></span><br><span class="line">    Bitmap picture;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack</tag>
        <tag>Room</tag>
      </tags>
  </entry>
  <entry>
    <title>Jetpack WorkManager</title>
    <url>/2020/01/07/Jetpack-WorkManager/</url>
    <content><![CDATA[]]></content>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack</tag>
      </tags>
  </entry>
  <entry>
    <title>Jetpack Paging</title>
    <url>/2020/01/07/Jetpack-Paging/</url>
    <content><![CDATA[]]></content>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack</tag>
      </tags>
  </entry>
  <entry>
    <title>Jetpack Navigation</title>
    <url>/2020/01/07/Jetpack-Navigation/</url>
    <content><![CDATA[]]></content>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack</tag>
      </tags>
  </entry>
  <entry>
    <title>Room概览</title>
    <url>/2020/01/07/Jetpack-Room/</url>
    <content><![CDATA[<p>Room 在 SQLite 上提供了一个抽象层，以便在充分利用 SQLite 的强大功能的同时，能够流畅地访问数据库。</p>
<p>处理大量结构化数据的应用可极大地受益于在本地保留这些数据，最常见的方法就是缓存相关数据。这样，当设备无法访问网络时，用户仍可在离线状态下浏览相应内容。之后设备重新连接到网络后，用户发起的所有内容更改都会同步到服务器。</p>
<a id="more"></a>
<p><strong>注意：要在应用中使用 Room，请在应用的 <code>build.gradle</code> 文件中声明 Room 依赖项。</strong></p>
<h2 id="声明依赖项"><a href="#声明依赖项" class="headerlink" title="声明依赖项"></a>声明依赖项</h2><p>要添加 Room 的依赖项，您必须将 Google Maven 代码库添加到项目中。</p>
<p>在应用或模块的 <code>build.gradle</code> 文件中添加所需工件的依赖项：</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">      <span class="keyword">def</span> room_version = <span class="string">"2.2.0-rc01"</span></span><br><span class="line"></span><br><span class="line">      implementation <span class="string">"androidx.room:room-runtime:$room_version"</span></span><br><span class="line">      annotationProcessor <span class="string">"androidx.room:room-compiler:$room_version"</span> <span class="comment">// For Kotlin use kapt instead of annotationProcessor</span></span><br><span class="line"></span><br><span class="line">      <span class="comment">// optional - Kotlin Extensions and Coroutines support for Room</span></span><br><span class="line">      implementation <span class="string">"androidx.room:room-ktx:$room_version"</span></span><br><span class="line"></span><br><span class="line">      <span class="comment">// optional - RxJava support for Room</span></span><br><span class="line">      implementation <span class="string">"androidx.room:room-rxjava2:$room_version"</span></span><br><span class="line"></span><br><span class="line">      <span class="comment">// optional - Guava support for Room, including Optional and ListenableFuture</span></span><br><span class="line">      implementation <span class="string">"androidx.room:room-guava:$room_version"</span></span><br><span class="line"></span><br><span class="line">      <span class="comment">// Test helpers</span></span><br><span class="line">      testImplementation <span class="string">"androidx.room:room-testing:$room_version"</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<h2 id="配置编译器选项"><a href="#配置编译器选项" class="headerlink" title="配置编译器选项"></a>配置编译器选项</h2><p>Room 具有以下注解处理器选项：</p>
<ul>
<li><p><code>room.schemaLocation</code>：配置并启用将数据库架构导出到给定目录中的 JSON 文件的功能。</p>
</li>
<li><p><code>room.incremental</code>：启用 Gradle 增量注解处理器。</p>
</li>
<li><p><code>room.expandProjection</code>：配置 Room 以重写查询，使其顶部星形投影在展开后仅包含 DAO 方法返回类型中定义的列。</p>
<p>以下代码段举例说明了如何配置这些选项：</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line">android &#123;</span><br><span class="line">        ...</span><br><span class="line">        defaultConfig &#123;</span><br><span class="line">            ...</span><br><span class="line">            javaCompileOptions &#123;</span><br><span class="line">                annotationProcessorOptions &#123;</span><br><span class="line">                    arguments = [</span><br><span class="line">                        <span class="string">"room.schemaLocation"</span>:<span class="string">"$projectDir/schemas"</span>.toString(),</span><br><span class="line">                        <span class="string">"room.incremental"</span>:<span class="string">"true"</span>,</span><br><span class="line">                        <span class="string">"room.expandProjection"</span>:<span class="string">"true"</span>]</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h2 id="组件"><a href="#组件" class="headerlink" title="组件"></a>组件</h2><p>Room 包含 3 个主要组件：</p>
<ol>
<li><p><strong>Database</strong>：包含数据库持有者，并作为应用已保留的持久关系型数据的底层连接的主要接入点。</p>
<p>使用<code>@Database</code>注释的类应满足以下条件：</p>
<ul>
<li>是扩展 <code>RoomDatabase</code>的抽象类</li>
<li>在注释中添加与数据库关联的实体列表</li>
<li>包含具有 0 个参数且返回使用 <code>@Dao</code>注释的类的抽象方法</li>
</ul>
<p>在运行时，您可以通过调用 <code>Room.databaseBuilder()</code>或 <code>Room.inMemoryDatabaseBuilder()</code>获取 <code>Database</code>的实例。</p>
</li>
<li><p><strong>Entity</strong>：表示数据库中的表。</p>
</li>
<li><strong>DAO</strong>：包含用于访问数据库的方法。</li>
</ol>
<p>应用使用 Room 数据库来获取与该数据库关联的数据访问对象 (DAO)。然后，应用使用每个 DAO 从数据库中获取实体，然后再将对这些实体的所有更改保存回数据库中。最后，应用使用实体来获取和设置与数据库中的表列相对应的值。</p>
<p>Room 不同组件之间的关系如图 1 所示：</p>
<p><img src="/2020/01/07/Jetpack-Room/room_architecture.png" alt="**图 1.** Room 架构图"></p>
<p>以下代码段包含具有一个实体和一个 DAO 的示例数据库配置。</p>
<p>User</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="meta">@PrimaryKey</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> uid;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ColumnInfo</span>(name = <span class="string">"first_name"</span>)</span><br><span class="line">    <span class="keyword">public</span> String firstName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ColumnInfo</span>(name = <span class="string">"last_name"</span>)</span><br><span class="line">    <span class="keyword">public</span> String lastName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>UserDao</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line">   <span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">UserDao</span> </span>&#123;</span><br><span class="line">       <span class="meta">@Query</span>(<span class="string">"SELECT * FROM user"</span>)</span><br><span class="line">       <span class="function">List&lt;User&gt; <span class="title">getAll</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">       <span class="meta">@Query</span>(<span class="string">"SELECT * FROM user WHERE uid IN (:userIds)"</span>)</span><br><span class="line">       <span class="function">List&lt;User&gt; <span class="title">loadAllByIds</span><span class="params">(<span class="keyword">int</span>[] userIds)</span></span>;</span><br><span class="line"></span><br><span class="line">       <span class="meta">@Query</span>(<span class="string">"SELECT * FROM user WHERE first_name LIKE :first AND "</span> +</span><br><span class="line">              <span class="string">"last_name LIKE :last LIMIT 1"</span>)</span><br><span class="line">       <span class="function">User <span class="title">findByName</span><span class="params">(String first, String last)</span></span>;</span><br><span class="line"></span><br><span class="line">       <span class="meta">@Insert</span></span><br><span class="line">       <span class="function"><span class="keyword">void</span> <span class="title">insertAll</span><span class="params">(User... users)</span></span>;</span><br><span class="line"></span><br><span class="line">       <span class="meta">@Delete</span></span><br><span class="line">       <span class="function"><span class="keyword">void</span> <span class="title">delete</span><span class="params">(User user)</span></span>;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
<p>AppDatabase</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Database</span>(entities = &#123;User.class&#125;, version = <span class="number">1</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AppDatabase</span> <span class="keyword">extends</span> <span class="title">RoomDatabase</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> UserDao <span class="title">userDao</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>创建上述文件后，您可以使用以下代码获取已创建的数据库的实例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">AppDatabase db = Room.databaseBuilder(getApplicationContext(),</span><br><span class="line">        AppDatabase.class, <span class="string">"database-name"</span>).build();</span><br></pre></td></tr></table></figure>
<p>注意：如果您的应用在单个进程中运行，则在实例化 <code>AppDatabase</code> 对象时应遵循单例设计模式。每个 <code>RoomDatabase</code>实例的成本相当高，而您几乎不需要在单个进程中访问多个实例。</p>
<p><strong>单例实现</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Database</span>(entities = &#123;User.class&#125;, version = <span class="number">1</span>, exportSchema = <span class="keyword">false</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">UserRoomDatabase</span> <span class="keyword">extends</span> <span class="title">RoomDatabase</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> UserDao <span class="title">userDao</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> UserRoomDatabase INSTANCE;</span><br><span class="line">   <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> NUMBER_OF_THREADS = <span class="number">4</span>;</span><br><span class="line">   <span class="keyword">static</span> <span class="keyword">final</span> ExecutorService databaseWriteExecutor =</span><br><span class="line">        Executors.newFixedThreadPool(NUMBER_OF_THREADS);</span><br><span class="line"></span><br><span class="line">   <span class="function"><span class="keyword">static</span> UserRoomDatabase <span class="title">getDatabase</span><span class="params">(<span class="keyword">final</span> Context context)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (INSTANCE == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (UserRoomDatabase.class) &#123;</span><br><span class="line">                <span class="keyword">if</span> (INSTANCE == <span class="keyword">null</span>) &#123;</span><br><span class="line">                    INSTANCE = Room.databaseBuilder(context.getApplicationContext(),</span><br><span class="line">                            UserRoomDatabase.class, <span class="string">"user_database"</span>)</span><br><span class="line">                        	.allowMainThreadQueries()<span class="comment">//允许在UI线程执行数据库操作</span></span><br><span class="line">                            .build();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以上实现了单例模式，可以在应用的Application通过UserRoomDatabase.getDatabase方法获取UserRoomDatabase的实例对象了。</p>
<p>当用户得到UserRoomDatabase对象之后，就可以通过上面的userDao()方法获取UserDao对象，然后就可以执行UserDao中的方法对数据库进行更删改查等操作了。</p>
<p>如果您的应用在多个进程中运行，请在数据库构建器调用中包含 <code>enableMultiInstanceInvalidation()</code>。这样，如果您在每个进程中都有一个 <code>AppDatabase</code> 实例，就可以在一个进程中使共享数据库文件失效，并且这种失效会自动传播到其他进程中的 <code>AppDatabase</code> 实例。</p>
]]></content>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack</tag>
        <tag>Room</tag>
      </tags>
  </entry>
  <entry>
    <title>时间控件TextClock</title>
    <url>/2018/08/04/TextClock-for-Time/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>各位大佬好，小弟最近做了一款安卓机顶盒的主页。因为是针对电视的主页，一般来说需要显示系统时间。糟糕的是，我在Android Studio里面我没有找到对应显示时间的控件，参考以前同事写的代码后采用了以下解决方案：<strong>利用定时器，每秒取一次系统时间，然后格式化并显示在TextView上。</strong>显然这种方式是不行的，那我们该怎么做呢？</p>
<a id="more"></a>
<h1 id="正题"><a href="#正题" class="headerlink" title="正题"></a>正题</h1><p>显然采用上面的解决方案能够解决问题，但是每秒都去新建Date对象，然后格式化成String肯定是很不行的。首先是定时器，其次便是每秒新增对象，就像个死循环一样，非常的消耗资源。于是通过查询网页和咨询网友，终于找到了比较好的解决方案，它就是我们的主角：<strong>TextClock</strong>！</p>
<p>在我们介绍TextClock之前，我们必须要先介绍下<em>DateFormat</em>和<em>日期时间模式字符串</em></p>
<h2 id="DateFormat"><a href="#DateFormat" class="headerlink" title="DateFormat"></a>DateFormat</h2><p><strong>DateFormat</strong>是用来格式化时间的，因为我们new出来的Date对象其实是个String类型的时间戳，而且每个用户需要显示的时间样式也不一样（根据时区），因此我们必须采用DateFormat对时间进行格式化。让我们来看下它的format方法：</p>
<p><img src="/2018/08/04/TextClock-for-Time/date_format.png" alt="Date_Format"></p>
<p>从第一个格式化方法我们不难看出，他需要两个参数，第二个就是我们的Date对象。那么第一个呢？其实就是我们需要如何显示时间的模式字符串。</p>
<h2 id="时间模式字符串"><a href="#时间模式字符串" class="headerlink" title="时间模式字符串"></a>时间模式字符串</h2><p>在<em>date and time pattern strings</em>中，<strong>未被单引号引用的字母从’A’ to ‘Z’和’a’ to ‘z’被解释为模式字母，代表着日期和时间的字符串</strong>，<strong>其他的字母或符号在格式化时只被单纯的复制而不进行解释。</strong></p>
<p>模式字母的定义如下图：</p>
<table>
<thead>
<tr>
<th>字母</th>
<th>含义</th>
<th>表示</th>
</tr>
</thead>
<tbody>
<tr>
<td>y</td>
<td>年</td>
<td>Year</td>
</tr>
<tr>
<td>M</td>
<td>年份中的月</td>
<td>Month</td>
</tr>
<tr>
<td>w</td>
<td>年中的周</td>
<td>number</td>
</tr>
<tr>
<td>W</td>
<td>月中的周</td>
<td>number</td>
</tr>
<tr>
<td>D</td>
<td>年中的天</td>
<td>number</td>
</tr>
<tr>
<td>d</td>
<td>月中的天</td>
<td>number</td>
</tr>
<tr>
<td>F</td>
<td>月中星期</td>
<td>number</td>
</tr>
<tr>
<td>E</td>
<td>周</td>
<td>Tuesday</td>
</tr>
<tr>
<td>a</td>
<td>AM/PM</td>
<td>上午下午</td>
</tr>
<tr>
<td>H</td>
<td>小时数（0-23）</td>
<td>number</td>
</tr>
<tr>
<td>k</td>
<td>小时数</td>
<td>number</td>
</tr>
<tr>
<td>K</td>
<td>12小时中的小时数（0-11）</td>
<td>number</td>
</tr>
<tr>
<td>h</td>
<td>12小时制中的小时数（1-12）</td>
<td>number</td>
</tr>
<tr>
<td>m</td>
<td>分钟</td>
<td>number</td>
</tr>
<tr>
<td>s</td>
<td>秒数</td>
<td>number</td>
</tr>
<tr>
<td>S</td>
<td>毫秒数</td>
<td>number</td>
</tr>
<tr>
<td>z</td>
<td>General time zone 时区</td>
<td>Pacific Standard Time; PST; GMT-08:00</td>
</tr>
<tr>
<td>Z</td>
<td>RFC 822 time zone 时区</td>
<td>-0800</td>
</tr>
<tr>
<td></td>
<td></td>
</tr>
</tbody>
</table>
<p><strong><em>举例子</em></strong></p>
<table>
<thead>
<tr>
<th>Date and Time Pattern</th>
<th>Result</th>
<th>备注</th>
</tr>
</thead>
<tbody>
<tr>
<td>yyyy-MM-dd</td>
<td>2018-08-04</td>
<td></td>
</tr>
<tr>
<td>EEE, MM d</td>
<td>Wed, Jul 4</td>
<td></td>
</tr>
<tr>
<td>HH:mm a</td>
<td>12:21 AM</td>
<td></td>
</tr>
<tr>
<td>yyyy:MM:dd HH:mm:ss</td>
<td>2018:08:04 10:21:23</td>
</tr>
</tbody>
</table>
<h2 id="TextClock"><a href="#TextClock" class="headerlink" title="TextClock"></a>TextClock</h2><p>好啦，说完了以上两个概念，现在我们具体来看看如何使用TextClock！</p>
<p>首先就当成普通的控件在布局文件中直接使用，如：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">TextClock</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:id</span>=<span class="string">"@+id/timeView"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:layout_width</span>=<span class="string">"wrap_content"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:layout_height</span>=<span class="string">"wrap_content"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:layout_centerVertical</span>=<span class="string">"true"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:layout_gravity</span>=<span class="string">"center"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:layout_toStartOf</span>=<span class="string">"@+id/dataView"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:format24Hour</span> =<span class="string">"HH:mm"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:format12Hour</span> =<span class="string">"hh:mm"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:textColor</span>=<span class="string">"@color/material_white"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:textSize</span>=<span class="string">"@dimen/w_56"</span> /&gt;</span></span><br></pre></td></tr></table></figure>
<p>与其他控件不同的是，TextClock有几个特别的属性，根据这些属性能够显示特定格式的时间。具体如下：</p>
<table>
<thead>
<tr>
<th>XML属性</th>
<th>相关方法</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>android:format12Hour</td>
<td>setFormat12Hour(CharSequence)</td>
<td>设置12小时制的格式字符串</td>
</tr>
<tr>
<td>android:format24Hour</td>
<td>setFormat12Hour(CharSequence)</td>
<td>设置24小时制的格式字符串</td>
</tr>
<tr>
<td>android:timeZone</td>
<td>setTimeZone(String)</td>
<td>设置该时钟的时区</td>
</tr>
</tbody>
</table>
<p>有时候为了同时支持24小时制和12小时制，建议上面的前两个属性都需要加上！</p>
<p>多学习、多实践以上内容后，用TextClock实现下面的布局就变得非常的容易，加油吧！！！！</p>
<p><img src="/2018/08/04/TextClock-for-Time/example.png" alt="example"></p>
]]></content>
      <tags>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo搭建个人博客</title>
    <url>/2018/07/28/Build-personal-blog-by-hexo/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>博客已经不是什么很新鲜的玩意儿了，尤其是当微信公众号、简书等优秀的写作平台的兴起，个人博客的价值已经越来越式微了。但是作为一名软件研发行业的小学生，拥有个人博客记录一些日常所学、平日所感还是感觉不错的，一直以来我都是利用Hexo生成博客并托管到Github等开源平台。这期间还挺折腾的，主要表现在：</p>
<ol>
<li>博客的迁移，从github迁移到gitlab，然后是coding，然后又迁移到github；</li>
<li>写过几篇文章，但是都感觉质量不高，而且很难坚持；</li>
<li>托管到第三方网站，依赖别人必将受限于人。</li>
</ol>
<p>因此这次痛下决心，自己购买了个阿里云服务器和域名，将hexo搭建的博客放在自己的服务器上，然后利用gitolite进行管理，nginx实现展示，下面分享给大家。</p>
<a id="more"></a>
<h1 id="正题"><a href="#正题" class="headerlink" title="正题"></a>正题</h1><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><p>hexo是一个快速、简洁且高效的博客框架，它使用<strong>Markdown</strong>解析文章，执行生成命令即可得到靓丽的静态页面。安装hexo非常的简单。首先必须安装以下两款软件：</p>
<ol>
<li><a href="https://nodejs.org/en/" target="_blank" rel="noopener">Node.js</a></li>
<li><a href="https://git-scm.com/" target="_blank" rel="noopener">Git</a></li>
</ol>
<p>至于如何安装软件这里就不做介绍了，windows用户直接下载安装，mac及linux用户通过命令行就好了，请自行百度~</p>
<p>当Git和Node.js都安装完成后，windows用户请打开gitbash（git的命令行工具），mac、linux请打开命令行，执行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br></pre></td></tr></table></figure>
<p>就完成了hexo的安装。</p>
<h3 id="建站"><a href="#建站" class="headerlink" title="建站"></a>建站</h3><p>安装hexo完成后，请执行下列命令，hexo将会在指定文件夹中新建所需要的文件。注：同样是在命令行下进行操作！</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo init &lt;folder&gt; //hexo初始化folder文件夹</span><br><span class="line">cd &lt;folder&gt;   //进入该文件夹</span><br></pre></td></tr></table></figure>
<p>新建完成后，进入指定文件夹，我们会看到下列文件：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.</span><br><span class="line">├── _config.yml //站点配置文件，比如配置主题等等</span><br><span class="line">├── package.json </span><br><span class="line">├── scaffolds  //模板文件夹</span><br><span class="line">├── source  //资源文件夹，新增的文章都会放在该文件夹中</span><br><span class="line">|   ├── _drafts</span><br><span class="line">|   └── _posts</span><br><span class="line">└── themes  //主题文件夹</span><br></pre></td></tr></table></figure>
<p>具体怎么配置以及修改主题请参考<strong><a href="https://hexo.io/zh-cn/docs/" target="_blank" rel="noopener">Hexo的官方文档</a></strong></p>
<h3 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo g //生成博客，是hexo generate的缩写</span><br><span class="line">//执行完该命令，会在该目录下新增public文件夹，该文件夹就是最终的博客内容。</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo s //预览博客，预览地址为http://localhost:4000/；是 hexo server的缩写</span><br><span class="line">//执行该命令，会启动本地web服务，用户博客的预览</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo d //发布博客到远端，如github等等，稍后会讲;是hexo deploy的缩写</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo new post &quot;testing&quot; </span><br><span class="line">//新建testing的文章，在source的_post文件夹下会生成testing.md文件</span><br></pre></td></tr></table></figure>
<p>那现在我们就来实践下：新增testing文章–&gt;生成博客–&gt;进行预览。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ hexo new post &quot;testing&quot;</span><br><span class="line">INFO  Created: ~\hello\source\_posts\testing.md</span><br><span class="line">$ hexo g</span><br><span class="line">INFO  Start processing</span><br><span class="line">INFO  Files loaded in 319 ms</span><br><span class="line">INFO  Generated: 2018/07/29/hello-world/index.html</span><br><span class="line">INFO  Generated: archives/2018/index.html</span><br><span class="line">INFO  Generated: index.html</span><br><span class="line">INFO  Generated: archives/index.html</span><br><span class="line">INFO  Generated: archives/2018/07/index.html</span><br><span class="line">INFO  Generated: css/fonts/FontAwesome.otf</span><br><span class="line">INFO  Generated: fancybox/fancybox_loading.gif</span><br><span class="line">INFO  Generated: fancybox/blank.gif</span><br><span class="line">INFO  Generated: fancybox/fancybox_overlay.png</span><br><span class="line">INFO  Generated: fancybox/fancybox_sprite@2x.png</span><br><span class="line">INFO  Generated: css/fonts/fontawesome-webfont.eot</span><br><span class="line">INFO  Generated: fancybox/fancybox_loading@2x.gif</span><br><span class="line">INFO  Generated: fancybox/helpers/fancybox_buttons.png</span><br><span class="line">INFO  Generated: fancybox/fancybox_sprite.png</span><br><span class="line">INFO  Generated: css/fonts/fontawesome-webfont.woff</span><br><span class="line">INFO  Generated: css/fonts/fontawesome-webfont.ttf</span><br><span class="line">INFO  Generated: fancybox/jquery.fancybox.css</span><br><span class="line">INFO  Generated: 2018/07/29/testing/index.html</span><br><span class="line">INFO  Generated: fancybox/helpers/jquery.fancybox-buttons.css</span><br><span class="line">INFO  Generated: fancybox/helpers/jquery.fancybox-buttons.js</span><br><span class="line">INFO  Generated: fancybox/helpers/jquery.fancybox-thumbs.css</span><br><span class="line">INFO  Generated: js/script.js</span><br><span class="line">INFO  Generated: css/style.css</span><br><span class="line">INFO  Generated: fancybox/helpers/jquery.fancybox-media.js</span><br><span class="line">INFO  Generated: fancybox/jquery.fancybox.js</span><br><span class="line">INFO  Generated: fancybox/helpers/jquery.fancybox-thumbs.js</span><br><span class="line">INFO  Generated: fancybox/jquery.fancybox.pack.js</span><br><span class="line">INFO  Generated: css/fonts/fontawesome-webfont.svg</span><br><span class="line">INFO  Generated: css/images/banner.jpg</span><br><span class="line">INFO  29 files generated in 710 ms</span><br><span class="line">$ hexo s</span><br><span class="line">INFO  Start processing</span><br><span class="line">INFO  Hexo is running at http://localhost:4000/. Press Ctrl+C to stop.</span><br></pre></td></tr></table></figure>
<p>然后打开浏览器，输入<a href="http://localhost:4000/，你就可以看到你的博客了，如下图：" target="_blank" rel="noopener">http://localhost:4000/，你就可以看到你的博客了，如下图：</a></p>
<p><img src="/2018/07/28/Build-personal-blog-by-hexo/./Build-personal-blog-by-hexo/testing.png" alt="testing"></p>
<h3 id="更换主题"><a href="#更换主题" class="headerlink" title="更换主题"></a>更换主题</h3><p>是不是觉得上面的默认主题很一般？其实网上有很多hexo的主题，比如next，material等等，hexo theme里面收录了很多优秀的主题，地址如下：<a href="https://hexo.io/themes/" target="_blank" rel="noopener">Hexo Theme</a></p>
<p>这里推荐几个常用的主题吧！</p>
<table>
<thead>
<tr>
<th>名称</th>
<th>地址</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong><a href="https://github.com/haojen/hexo-theme-Anisina" target="_blank" rel="noopener">Anisina</a></strong></td>
<td><a href="https://github.com/Haojen/hexo-theme-Anisina" target="_blank" rel="noopener">https://github.com/Haojen/hexo-theme-Anisina</a></td>
</tr>
<tr>
<td><strong>material</strong></td>
<td><a href="https://github.com/viosey/hexo-theme-material" target="_blank" rel="noopener">https://github.com/viosey/hexo-theme-material</a></td>
</tr>
<tr>
<td><strong>next</strong></td>
<td><a href="https://github.com/iissnan/hexo-theme-next" target="_blank" rel="noopener">https://github.com/iissnan/hexo-theme-next</a></td>
</tr>
<tr>
<td><strong>beantech</strong></td>
<td><a href="https://github.com/YenYuHsuan/hexo-theme-beantech/" target="_blank" rel="noopener">https://github.com/YenYuHsuan/hexo-theme-beantech/</a></td>
</tr>
</tbody>
</table>
<p>其实每个主题的文档都会告诉用户如何去更换主题的，这里指针对next进行介绍。</p>
<p>首先你需要进入博客目录，然后执行下面的操作：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ cd your-hexo-site</span><br><span class="line">$ git clone https://github.com/iissnan/hexo-theme-next themes/next</span><br><span class="line">//或者你可以直接将该主题从github下载下来，然后放到theme文件夹下，都是一样的。</span><br></pre></td></tr></table></figure>
<p>那么在theme目录中就存在next的文件夹了，也就是我们的主题；接下来只需要修改博客的根目录的_config.yml文件并进行如下修改：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">theme: next//找到theme关键字，讲landscape改成next即可</span><br></pre></td></tr></table></figure>
<p>至此利用hexo搭建博客，修改主题已经完成了，剩下的就是写文章，不断的丰富博客的内容了。</p>
<h2 id="预告"><a href="#预告" class="headerlink" title="预告"></a>预告</h2><p>接下来的一篇文章会介绍，如何利用个人Git服务器管理博客源文件和博客最终文件，并利用nginx作为web服务器显示博客并进行自动更新~ </p>
<p>精彩内容，不容错过！！！！！</p>
]]></content>
      <tags>
        <tag>博客</tag>
      </tags>
  </entry>
  <entry>
    <title>使用gitolite搭建Git服务器</title>
    <url>/2018/07/27/use-gitolite-to-manage-project/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>2018年4月底入职一家研发智能机顶盒的公司，7月中旬一位大神离职了，然后进行工作交接，其中就涉及到了如何利用Gitolite管理项目。Git操作使用的很多，但是如何搭建Git服务器以及如何进行管理却还没有接触过，因此利用闲暇时间摸索了下并记录下来~~<br>我个人买了个阿里云的服务器，一年是293￥，其实也不算贵了。 主要原因是：</p>
<ol>
<li>这毕竟是自己个人学习的内容，当然不能直接在公司服务器进行测试了；</li>
<li>以前将个人博客托管在github或者gitlab，总是会丢数据，所以打算将博客搭建到个人服务器，方便管理；</li>
<li>后期如果精力的话会实现一些个人项目，可以放在自己的服务器中。</li>
</ol>
<a id="more"></a> 
<h1 id="正题"><a href="#正题" class="headerlink" title="正题"></a>正题</h1><p>言归正传，我们现在开始着手搭建Git服务器，主要流程有新增用户-&gt;安装软件-&gt;配置SSH KEY-&gt;管理员配置-&gt;新增用户和项目。</p>
<h2 id="新增服务器用户"><a href="#新增服务器用户" class="headerlink" title="新增服务器用户"></a>新增服务器用户</h2><p><strong>初始环境</strong></p>
<table>
<thead>
<tr>
<th>名称</th>
<th style="text-align:center">详情</th>
</tr>
</thead>
<tbody>
<tr>
<td>服务器</td>
<td style="text-align:center">阿里云</td>
</tr>
<tr>
<td>配置</td>
<td style="text-align:center">1核 1G内存 40G</td>
</tr>
<tr>
<td>操作系统</td>
<td style="text-align:center">Ubuntu 16.04</td>
</tr>
</tbody>
</table>
<p>默认情况下服务器只有root用户，直接用root进行操作肯定是不安全的，因此需要额外新增两个账户。一个用来存放Git仓库（用户名为git），一个用来进行Git用户管理（用户名为git_admin）。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">useradd git -m //添加git用户</span><br><span class="line">passwd git//设置git用户的登陆密码</span><br><span class="line"></span><br><span class="line">useradd git_admin -m //添加git_admin用户</span><br><span class="line">passwd git_admin //设置git_admin的登陆密码</span><br></pre></td></tr></table></figure>
<h2 id="安装软件"><a href="#安装软件" class="headerlink" title="安装软件"></a>安装软件</h2><p><strong>安装Git</strong></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-get update//更新软件源</span><br><span class="line">sudo apt-get install git</span><br></pre></td></tr></table></figure>
<p><strong>生成sshkey</strong><br>切换到git_admin用户下，然后执行ssh-keygen生成sshkey，具体操作如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo su git_admin//切换到git_admin</span><br><span class="line">ssh-keygen -t rsa -C "example@email.com"</span><br></pre></td></tr></table></figure>
<p>这时候一直按回车就可以了，在你的根目录下就会生成.ssh文件夹，你可以通过<strong>ls -al</strong>来进行查看！将.ssh<br>文件夹下的id_rsa.pub文件拷贝到/tmp目录下备用。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cp ~/.ssh/id_rsa.pub /tmp</span><br></pre></td></tr></table></figure>
<p><strong>安装gitolite</strong><br>切换到git用户，并新建bin目录，操作如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">su git //切换到git用户</span><br><span class="line">cd ~ //进入git用户目录</span><br><span class="line">mkdir bin//新建bin文件夹'</span><br></pre></td></tr></table></figure>
<p>下载gitolite代码</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git clone http://github.com/sitaramc/gitolite</span><br></pre></td></tr></table></figure>
<p>安装gitolite到bin目录</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span>&#123;HOME&#125;/gitolite/install -to $&#123;HOME&#125;/bin</span><br></pre></td></tr></table></figure>
<p>设置管理员ssh key</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$&#123;HOME&#125;/bin/gitolite setup -pk /tmp/id_rsa.pub</span><br></pre></td></tr></table></figure>
<p>执行上面的步骤后，在git的根目录下会生成<strong>projects.list</strong>文件和<strong>repositories</strong>文件夹，projects.list就是描述仓库列表，repositories就是我们的项目仓库了。在repositories文件夹下面默认会有两个仓库，分别是gitolite-admin.git和testing.git。前者我们等下会讲到，利用gitolite-admin我们可以新增项目并管理用户，后者只是提供测试而已。</p>
<p>至此我们gitolite的安装其实已经完成了，那么下一步我们将介绍如何添加项目，用户以及配置项目用户权限。</p>
<h2 id="管理员配置"><a href="#管理员配置" class="headerlink" title="管理员配置"></a>管理员配置</h2><p>切换到git_admin用户，然后将git用户下的git-admin.git仓库clone下来，具体操作如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo su git_admin//切换到git_admin用户</span><br><span class="line"><span class="built_in">cd</span> ~//进入到git_admin的用户目录/home/git_admin</span><br><span class="line">git <span class="built_in">clone</span> git@服务器ip地址(或者本机名称):gitolite-admin.git//<span class="built_in">clone</span> gitolite-admin项目</span><br></pre></td></tr></table></figure>
<p>在gitolite目录下存在<strong>conf</strong>与<strong>keydir</strong>两个目录，其中conf/下面有个名为gitolite.conf的配置文件。 </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">conf/gitolite.conf 用于Git项目配置，访问权限设置。</span><br><span class="line">keydir/ 用于存储用户的SSH public key(公钥）。</span><br></pre></td></tr></table></figure>
<p>管理员利用keydir可以增加与删除用户，利用conf就可以增加、删除项目并给用户配置不同的项目操作权限。</p>
<h2 id="新增用户"><a href="#新增用户" class="headerlink" title="新增用户"></a>新增用户</h2><p>如果要新增用户，该怎么操作呢？其实很简单，只需要用户通过ssh-keygen生成相应的公钥与密钥，然后将公钥交给我们的管理员，管理员修改公钥名称（为了区分不同的用户，因此需要重命名），最后放到keydir目录下就可以了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">用户生成公钥(id_rsa.pub)---&gt;交给管理员----&gt;修改名称(如：zhangsan.pub)---&gt;放到keydir目录下---&gt;提交到服务器（git commit and push）</span><br></pre></td></tr></table></figure>
<h2 id="新增项目"><a href="#新增项目" class="headerlink" title="新增项目"></a>新增项目</h2><p>那如何新增项目呢？还记得默认有个testing的项目吗，我们完全可以依葫芦画瓢来新增项目。首先让我们来瞅一瞅conf/gitolite.conf文件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">repo gitolite-admin</span><br><span class="line">    RW+     =   admin</span><br><span class="line"></span><br><span class="line">repo testing</span><br><span class="line">    RW+     =   @all</span><br></pre></td></tr></table></figure>
<p>R表示读权限，W表示写权限；@all表示所有的用户都对testing项目具有读写权限；因为我将git_admin的sshkey命名为admin.pub，所以gitolite-admin的读写用户为admin。这里我们可以得出结论，我们是根据keydir目录中的key的名称来区分不同的用户，比如zhangsan.pub放在keydir下，那么该用户名为zhangsan！！！！</p>
<p>假设我们要新增只有zhangsan才能读写的名为HelloWorld的项目，那么只需要这样写：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">repo HelloWorld</span><br><span class="line">	RW+     =   zhangsan</span><br></pre></td></tr></table></figure>
<p>当你推送到远程仓库后，在git用户的repositories目录下就会新增一个HelloWorld.git远程仓库，张三可以通过git clone就可以将项目clone下来了。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git clone git@server_ip:HelloWorld.git</span><br></pre></td></tr></table></figure>
<p><strong>注意：无论是你添加用户还是新增项目，你都需要git提交到远程仓库中。当你提交项目时，git用户目录下的repositories 就会新增该项目仓库。</strong></p>
<p>至此我们就利用gitolite简单的搭建了Git服务器进行用户与项目管理了，当然还有很多高深的配置与操作还有待去学习和发现。请参考官网：<a href="http://gitolite.com/gitolite/index.html" target="_blank" rel="noopener">Gitolite</a></p>
]]></content>
      <tags>
        <tag>Git相关</tag>
      </tags>
  </entry>
</search>
